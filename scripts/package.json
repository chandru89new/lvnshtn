{
  "name": "wordladder",
  "version": "0.3.0",
  "devDependencies": {
    "esbuild": "0.25.10",
    "purescript": "^0.15.15",
    "spago": "^0.21.0"
  },
  "scripts": {
    "bundle": "rm -rf dist && spago build && node bundle.js && chmod +x ./dist/wordladder"
  },
  "bin": {
    "wordladder": "dist/wordladder"
  },
  "files": [
    "dist",
    "readme.md"
  ]
}